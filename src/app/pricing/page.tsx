import { Pricing } from '@/components';

export const metadata = {
  title: 'Pricing | PoliGap AI',
  description: 'Flexible plans that scale with your compliance needs',
};

export default function PricingPage() {
  return (
    <main>
      <Pricing />
    </main>
  );
}
